import { useEffect } from "react";
import homeStyles from "../../stylesheets/Home/home.module.css"

// Home Screen will display:
// - If personal user: Order, On-Trend Food, Shop that is placed high in ranks
// - If shop user: Edit Food, Order, Menu, ...

const HomeScreen = () => {

  return (
    <div className="home-screen" style={homeStyles}>
      trần lưu quốc tuấn
    </div>
  )
};

export default HomeScreen;
